[build]
    command = "cp -r ./ ~ && cd ~ && pwd && chmod -R 755 /opt/buildhome"
    functions = "functions"
    publish = "/"
[[redirects]]
    from = "/api/*"
    to = "/.netlify/functions/:splat"
    status = 200

[[redirects]]
    from = "/*"
    to = "/"
    status = 200